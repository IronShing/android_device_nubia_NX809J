// ---- NX809J display SERVICES: PURE SOURCE (composer/allocator/demura) ----
// All three build from hardware/qcom-caf/sm8750/display and ship their own .rc + vintf,
// coherent with the source libsdm* stack (source libsdmutils exports sdm::IsExtendedRange,
// GLOBAL DEFAULT). composer_version=v3_3 (vendor_hals.mk) makes the source composer concrete.
// No composer prebuilt block: the @4 prebuilt is a version island (needs config-V13/
// composer3-V4 the source tree doesn't build). Source composer emits composer3 @3 (< FCM @4,
// a check_vintf release-gate warning, not a runtime boot gate).

prebuilt_etc {
    name: "mapper.qti.xml.nx809j",
    owner: "nubia",
    src: "proprietary/vendor/etc/vintf/manifest/mapper.qti.xml",
    filename: "mapper.qti.nx809j.xml",
    sub_dir: "vintf/manifest",
    soc_specific: true,
}

// init.qti.display_boot.sh + .rc: pulled from the sm8750 display source module via
